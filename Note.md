private Long statusId; -- auto genearted 
private MediaType StatusContent;  wrong instead of storing entire byte[] just aws url data
private ContentType StatusType; IMAGE or VIDEO data
private LocalDateTime createdAt; genearte 
private LocalDateTime expiredAt; after 24 hours
private int userId; for now get by user db later use contextholder pathvariable userId
private int views;  for now 1
private int stars; for now 0
private String objectKey; generated by method.

private String statusUrl;

byte[] status content;
status type;
pathVariable userId;

# Here one question is it manually deletion or aws and postgres sql can provide any approaches to automation.

1. user status upload.
2. In retrieve only fetch non expired status
// This is work on mid nights triggered by aws lambda
3. create one method for deletion of status based on expired date.

Status userId userDetails approche fetch random 
